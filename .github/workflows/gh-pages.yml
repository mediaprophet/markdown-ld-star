name: Skip GitHub Pages Deployment

on:
  push:
    branches: [main]

jobs:
  skip-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: No deployment needed
        run: echo "No site to deploy at this time. Skipping GitHub Pages deployment."
